---
title: SokurenkoDEV
description: Quick, accurate cheat sheets for the languages, frameworks, and tools web developers use every day — verified against primary sources, and typeset for your Kindle.
template: splash
# An empty `hero:` block is enough to route Starlight into its
# single-ContentPanel hero layout (Page.astro), avoiding both the
# redundant PageTitle and the border-top divider CSS draws between two
# adjacent .content-panel elements. The Hero component itself is
# overridden to render nothing — HomeHero.astro below is our real hero.
hero:
  tagline: ''
---

import HomeHero from '../../components/home/HomeHero.astro';
import SectionGrid from '../../components/home/SectionGrid.astro';
import KindleBand from '../../components/home/KindleBand.astro';

<HomeHero />
<SectionGrid />
<KindleBand />
